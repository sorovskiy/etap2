CREATE TABLE states(
                         id INTEGER PRIMARY KEY AUTOINCREMENT,
                         application TEXT,
                         param1 TEXT,
                         param2 INTEGER,
                         version INTEGER
);